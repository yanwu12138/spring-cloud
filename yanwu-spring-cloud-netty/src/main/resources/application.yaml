server:
  port: 8883
spring:
  application:
    name: yanwu-netty
  ### seata 配置
  cloud:
    alibaba:
      seata:
        tx-service-group: business-service-fescar-service-group
eureka:
  client:
    serviceUrl:
      defaultZone: http://127.0.0.1:8880/eureka/
logging:
  file: /log/yanwu-netty.log
  level:
    com: info
    org: info
    cn: info
netty:
  port: 6000